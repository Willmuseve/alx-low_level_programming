#include "main.h"
/**
 * main - tests a function that prints if integer is positive or negative
 * Description: a function that will do the printing
 * @i: has value
 * Return: 0
 */
int main(void)
{
	int i = 0;

	positive_or_negative(i);
	return (0);
}
