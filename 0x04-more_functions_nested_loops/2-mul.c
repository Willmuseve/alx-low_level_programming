#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers
 * @a: has a value of one integer
 * @b: has a value of the other integer
 *
 * Return: returns results
 */
int mul(int a, int b)
{
	return (a * b);
}
