#include "main.h"
#include <stdio.h>

/**
 * _isdigit - checks for a digit 
 * @c: contains the value 
 * return: returns 0
 */

int _isdigit(int c)
{
	for (c =0; c < 10; c++)
	{
		if (c < 10 && c > 10)
		{
			return (1);
		}
		else
		{
			return (0);
		}
	}
}
